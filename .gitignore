# =============================================================================
# Godot Engine 4.x — artefatos locais que NÃO devem subir pro repositório
# =============================================================================

# Cache de import + dados gerados automaticamente pelo editor (4.x)
.godot/

# Pasta de import legado (Godot 3.x) — incluída por segurança caso alguém
# abra o projeto numa versão antiga
.import/

# .import companions de assets em documentation/ — Godot gera um .import pra
# CADA PNG que detecta no projeto, inclusive screenshots de docs (que não são
# carregados via res://). Esses .import poluem o status sem agregar valor.
# Os .png originais continuam tracked normalmente.
documentation/**/*.import

# Builds e configs de export específicos do ambiente local
/android/
export.cfg
export_presets.cfg

# Arquivos de tradução compilados — gerados a partir dos .po/.csv
*.translation

# =============================================================================
# Sistema operacional
# =============================================================================
.DS_Store
Thumbs.db
desktop.ini

# =============================================================================
# Editores comuns
# =============================================================================
.vscode/
.idea/
*.swp

# =============================================================================
# Assets brutos — vivem FORA do repo em C:\Projetos\naruto-game-assets-brutos\
# (defesa caso voltem por engano)
# =============================================================================
NOPA/
naruto_sprites.zip

# Estado local do Claude Code (permissões aprovadas nesta máquina)
.claude/settings.local.json
